# BusControl PRO 2.1.2

Исправлена резервная копия:

- `PRAGMA wal_checkpoint(FULL)` выполняется через `rawQuery`;
- база закрывается перед копированием и снова открывается после него;
- восстановление проверяет заголовок SQLite;
- удаляются старые файлы `-wal`, `-shm` и `-journal`;
- восстановление выполняется через временный файл.
