BusControl PRO — iOS Live Activity iOS 16.1 build fix

Replace files preserving folders, then run flutter analyze and push to GitHub Actions.
Fix: uses the ActivityKit end(using:dismissalPolicy:) API available from iOS 16.1 instead of the newer iOS 16.2 end(_:dismissalPolicy:) overload.
