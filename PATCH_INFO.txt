BusControl PRO — SideStore/App Group build fix

This patch does NOT change widget tap behavior.
It only changes .github/workflows/ios-release.yml so the unsigned Flutter build
is ad-hoc signed with the existing Runner and Widget Extension entitlements
before the IPA is packaged. This preserves the App Group capability for the
SideStore re-signing pipeline and prints/validates both entitlements in CI logs.

Apply over C:\Projects\bus_control_pro_final and push.
