BusControl PRO - Widget visual toggle fix

Changes:
- Morning/evening controls use SwiftUI Toggle(isOn:intent:) instead of Button(intent:).
- The checkmark updates optimistically immediately when tapped.
- Existing ToggleTripIntent and app/database synchronization remain unchanged.

Copy this patch over C:\Projects\bus_control_pro_final with replacement.
